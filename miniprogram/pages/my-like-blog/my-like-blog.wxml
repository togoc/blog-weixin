<div class="h5-my-blog">
  <scroll-view scroll-y class="my-blog-container">
    <my-blog-item blog="{{item}}" wx:for="{{blogs}}" wx:key="_id" />
    <no-data wx:if="{{!blogs||!blogs.length}}">请尝试下拉刷新.</no-data>
  </scroll-view>
</div>
